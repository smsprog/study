<?xml version="1.0"?>
<project name="test" default="all" basedir=".">
  <property name="src"   value="."/>
  <property name="build" value="."/>
  <property name="lib"   value="."/>

<target name="all" depends="compile" description="Builds the whole project">
    <echo>Doing all</echo>
  </target>

<target name="compile" description="compile whole project">
    <echo>compile ${ant.project.name} </echo>
	<javac srcdir="${src}" destdir="${build}" includes="uobject/*.java"/>
	<javac srcdir="${src}" destdir="${build}" includes="imovable/*.java"/>
	<javac srcdir="${src}" destdir="${build}" includes="irotatable/*.java"/>
	<javac srcdir="${src}" destdir="${build}" includes="iburnable/*.java"/>
	<javac srcdir="${src}" destdir="${build}" includes="icommand/*.java"/>
	<javac srcdir="${src}" destdir="${build}" includes="dz1.java"/>
    <javac srcdir="${src}" destdir="${build}" includes="dz4.java"/>
  </target>
</project>
